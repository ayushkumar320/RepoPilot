# Graphify cache
graphify-out/cache/

# Graphify generated metadata (machine-local, do not commit)
graphify-out/.graphify_analysis.json
graphify-out/.graphify_semantic_marker
graphify-out/.graphify_labels.json
graphify-out/.graphify_root

# Graphify dated backups (auto-created on each update)
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# Graphify large/regenerable visualization (rebuild with `graphify update .`)
graphify-out/graph.html

# Committed shared artifacts (kept on purpose): graph.json, manifest.json, GRAPH_REPORT.md

# Logs
graphify.log

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
.python-version.local

# uv: keep uv.lock committed for reproducible installs.

# Local LLM cache
.cache/

# Env
.env
.env.local
.env.*.local

# Node
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Eval reports (timestamped JSON+Markdown produced by `python -m repopilot_evals run --report`)
eval-reports/
