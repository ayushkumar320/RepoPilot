{"claim": "httpx.BasicAuth attaches an Authorization header built from a base64-encoded 'username:password' pair.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_auth.py", "start_line": 126, "end_line": 143, "symbol": "BasicAuth"}, "content": "class BasicAuth(Auth):\n    \"\"\"\n    Allows the 'auth' argument to be passed as a (username, password) pair,\n    and uses HTTP Basic authentication.\n    \"\"\"\n\n    def __init__(self, username: str | bytes, password: str | bytes) -> None:\n        self._auth_header = self._build_auth_header(username, password)\n\n    def auth_flow(self, request: Request) -> typing.Generator[Request, Response, None]:\n        request.headers[\"Authorization\"] = self._auth_header\n        yield request\n\n    def _build_auth_header(self, username: str | bytes, password: str | bytes) -> str:\n        userpass = b\":\".join((to_bytes(username), to_bytes(password)))\n        token = b64encode(userpass).decode()\n        return f\"Basic {token}\"\n", "summary": "BasicAuth class — builds and yields a Basic Authorization header.", "kind": "function"}]}
{"claim": "The GZipDecoder uses zlib.decompressobj to decode gzip-encoded response bodies.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_decoders.py", "start_line": 85, "end_line": 105, "symbol": "GZipDecoder"}, "content": "class GZipDecoder(ContentDecoder):\n    \"\"\"\n    Handle 'gzip' decoding.\n\n    See: https://stackoverflow.com/questions/1838699\n    \"\"\"\n\n    def __init__(self) -> None:\n        self.decompressor = zlib.decompressobj(zlib.MAX_WBITS | 16)\n\n    def decode(self, data: bytes) -> bytes:\n        try:\n            return self.decompressor.decompress(data)\n        except zlib.error as exc:\n            raise DecodingError(str(exc)) from exc\n\n    def flush(self) -> bytes:\n        try:\n            return self.decompressor.flush()\n        except zlib.error as exc:  # pragma: no cover\n            raise DecodingError(str(exc)) from exc", "summary": "GZip content decoder using zlib.", "kind": "function"}]}
{"claim": "Client._send_handling_redirects raises TooManyRedirects once the redirect history exceeds max_redirects.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 964, "end_line": 999, "symbol": "Client._send_handling_redirects"}, "content": "    def _send_handling_redirects(\n        self,\n        request: Request,\n        follow_redirects: bool,\n        history: list[Response],\n    ) -> Response:\n        while True:\n            if len(history) > self.max_redirects:\n                raise TooManyRedirects(\n                    \"Exceeded maximum allowed redirects.\", request=request\n                )\n\n            for hook in self._event_hooks[\"request\"]:\n                hook(request)\n\n            response = self._send_single_request(request)\n            try:\n                for hook in self._event_hooks[\"response\"]:\n                    hook(response)\n                response.history = list(history)\n\n                if not response.has_redirect_location:\n                    return response\n\n                request = self._build_redirect_request(request, response)\n                history = history + [response]\n\n                if follow_redirects:\n                    response.read()\n                else:\n                    response.next_request = request\n                    return response\n\n            except BaseException as exc:\n                response.close()\n                raise exc", "summary": "Redirect loop that enforces a maximum redirect count.", "kind": "function"}]}
{"claim": "Limits exposes max_connections, max_keepalive_connections, and keepalive_expiry to configure connection-pool behaviour.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 159, "end_line": 196, "symbol": "Limits"}, "content": "class Limits:\n    \"\"\"\n    Configuration for limits to various client behaviors.\n\n    **Parameters:**\n\n    * **max_connections** - The maximum number of concurrent connections that may be\n            established.\n    * **max_keepalive_connections** - Allow the connection pool to maintain\n            keep-alive connections below this point. Should be less than or equal\n            to `max_connections`.\n    * **keepalive_expiry** - Time limit on idle keep-alive connections in seconds.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        max_connections: int | None = None,\n        max_keepalive_connections: int | None = None,\n        keepalive_expiry: float | None = 5.0,\n    ) -> None:\n        self.max_connections = max_connections\n        self.max_keepalive_connections = max_keepalive_connections\n        self.keepalive_expiry = keepalive_expiry\n\n    def __eq__(self, other: typing.Any) -> bool:\n        return (\n            isinstance(other, self.__class__)\n            and self.max_connections == other.max_connections\n            and self.max_keepalive_connections == other.max_keepalive_connections\n            and self.keepalive_expiry == other.keepalive_expiry\n        )\n\n    def __repr__(self) -> str:\n        class_name = self.__class__.__name__\n        return (\n            f\"{class_name}(max_connections={self.max_connections}, \"\n            f\"max_keepalive_connections={self.max_keepalive_connections}, \"", "summary": "Configuration object for connection-pool limits.", "kind": "function"}]}
{"claim": "Client._set_timeout writes a timeout entry into request.extensions when one is not already provided.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 584, "end_line": 592, "symbol": "BaseClient._set_timeout"}, "content": "    def _set_timeout(self, request: Request) -> None:\n        if \"timeout\" not in request.extensions:\n            timeout = (\n                self.timeout\n                if isinstance(self.timeout, UseClientDefault)\n                else Timeout(self.timeout)\n            )\n            request.extensions = dict(**request.extensions, timeout=timeout.as_dict())\n", "summary": "Merges the client's default timeout into request extensions.", "kind": "function"}]}
{"claim": "HTTPTransport.handle_request builds an httpcore.Request from the httpx.Request and dispatches it through self._pool.handle_request.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_transports/default.py", "start_line": 230, "end_line": 259, "symbol": "HTTPTransport.handle_request"}, "content": "    def handle_request(\n        self,\n        request: Request,\n    ) -> Response:\n        assert isinstance(request.stream, SyncByteStream)\n        import httpcore\n\n        req = httpcore.Request(\n            method=request.method,\n            url=httpcore.URL(\n                scheme=request.url.raw_scheme,\n                host=request.url.raw_host,\n                port=request.url.port,\n                target=request.url.raw_path,\n            ),\n            headers=request.headers.raw,\n            content=request.stream,\n            extensions=request.extensions,\n        )\n        with map_httpcore_exceptions():\n            resp = self._pool.handle_request(req)\n\n        assert isinstance(resp.stream, typing.Iterable)\n\n        return Response(\n            status_code=resp.status,\n            headers=resp.headers,\n            stream=ResponseStream(resp.stream),\n            extensions=resp.extensions,\n        )", "summary": "Sync transport: forwards request to httpcore connection pool.", "kind": "function"}]}
{"claim": "codes.is_success returns True for any HTTP status code in the 2xx range.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_status_codes.py", "start_line": 53, "end_line": 59, "symbol": "codes.is_success"}, "content": "    def is_success(cls, value: int) -> bool:\n        \"\"\"\n        Returns `True` for 2xx status codes, `False` otherwise.\n        \"\"\"\n        return 200 <= value <= 299\n\n    @classmethod", "summary": "Classifier helper returning True for 200..299 status codes.", "kind": "function"}]}
{"claim": "codes.is_redirect returns True for HTTP status codes in the 3xx range.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_status_codes.py", "start_line": 60, "end_line": 66, "symbol": "codes.is_redirect"}, "content": "    def is_redirect(cls, value: int) -> bool:\n        \"\"\"\n        Returns `True` for 3xx status codes, `False` otherwise.\n        \"\"\"\n        return 300 <= value <= 399\n\n    @classmethod", "summary": "Classifier helper for 300..399 status codes.", "kind": "function"}]}
{"claim": "ZStandardDecoder requires the optional `zstandard` package and decodes RFC 8878 zstd-encoded bodies.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_decoders.py", "start_line": 161, "end_line": 201, "symbol": "ZStandardDecoder"}, "content": "class ZStandardDecoder(ContentDecoder):\n    \"\"\"\n    Handle 'zstd' RFC 8878 decoding.\n\n    Requires `pip install zstandard`.\n    Can be installed as a dependency of httpx using `pip install httpx[zstd]`.\n    \"\"\"\n\n    # inspired by the ZstdDecoder implementation in urllib3\n    def __init__(self) -> None:\n        if zstandard is None:  # pragma: no cover\n            raise ImportError(\n                \"Using 'ZStandardDecoder', ...\"\n                \"Make sure to install httpx using `pip install httpx[zstd]`.\"\n            ) from None\n\n        self.decompressor = zstandard.ZstdDecompressor().decompressobj()\n        self.seen_data = False\n\n    def decode(self, data: bytes) -> bytes:\n        assert zstandard is not None\n        self.seen_data = True\n        output = io.BytesIO()\n        try:\n            output.write(self.decompressor.decompress(data))\n            while self.decompressor.eof and self.decompressor.unused_data:\n                unused_data = self.decompressor.unused_data\n                self.decompressor = zstandard.ZstdDecompressor().decompressobj()\n                output.write(self.decompressor.decompress(unused_data))\n        except zstandard.ZstdError as exc:\n            raise DecodingError(str(exc)) from exc\n        return output.getvalue()\n\n    def flush(self) -> bytes:\n        if not self.seen_data:\n            return b\"\"\n        ret = self.decompressor.flush()  # note: this is a no-op\n        if not self.decompressor.eof:\n            raise DecodingError(\"Zstandard data is incomplete\")  # pragma: no cover\n        return bytes(ret)\n", "summary": "Zstandard content decoder, RFC 8878.", "kind": "function"}]}
{"claim": "DigestAuth.auth_flow re-issues a request with an Authorization header after receiving a 401 with a 'WWW-Authenticate: Digest' challenge.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_auth.py", "start_line": 193, "end_line": 222, "symbol": "DigestAuth.auth_flow"}, "content": "    def auth_flow(self, request: Request) -> typing.Generator[Request, Response, None]:\n        if self._last_challenge:\n            request.headers[\"Authorization\"] = self._build_auth_header(\n                request, self._last_challenge\n            )\n\n        response = yield request\n\n        if response.status_code != 401 or \"www-authenticate\" not in response.headers:\n            # If the response is not a 401 then we don't\n            # need to build an authenticated request.\n            return\n\n        for auth_header in response.headers.get_list(\"www-authenticate\"):\n            if auth_header.lower().startswith(\"digest \"):\n                break\n        else:\n            # If the response does not include a 'WWW-Authenticate: Digest ...'\n            # header, then we don't need to build an authenticated request.\n            return\n\n        self._last_challenge = self._parse_challenge(request, response, auth_header)\n        self._nonce_count = 1\n\n        request.headers[\"Authorization\"] = self._build_auth_header(\n            request, self._last_challenge\n        )\n        if response.cookies:\n            Cookies(response.cookies).set_cookie_header(request=request)\n        yield request", "summary": "Digest auth flow that retries on 401 with computed credentials.", "kind": "function"}]}
{"claim": "Client._send_single_request obtains a transport via _transport_for_url and calls its handle_request method.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 1001, "end_line": 1035, "symbol": "Client._send_single_request"}, "content": "    def _send_single_request(self, request: Request) -> Response:\n        \"\"\"\n        Sends a single request, without handling any redirections.\n        \"\"\"\n        transport = self._transport_for_url(request.url)\n        start = time.perf_counter()\n\n        if not isinstance(request.stream, SyncByteStream):\n            raise RuntimeError(\n                \"Attempted to send an async request with a sync Client instance.\"\n            )\n\n        with request_context(request=request):\n            response = transport.handle_request(request)\n\n        assert isinstance(response.stream, SyncByteStream)\n\n        response.request = request\n        response.stream = BoundSyncStream(\n            response.stream, response=response, start=start\n        )\n        self.cookies.extract_cookies(response)\n        response.default_encoding = self._default_encoding\n\n        logger.info(\n            'HTTP Request: %s %s \"%s %d %s\"',\n            request.method,\n            request.url,\n            response.http_version,\n            response.status_code,\n            response.reason_phrase,\n        )\n\n        return response\n", "summary": "Send a single request, no redirect handling.", "kind": "function"}]}
{"claim": "Timeout.as_dict returns a dict with connect, read, write, and pool keys.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 132, "end_line": 138, "symbol": "Timeout.as_dict"}, "content": "    def as_dict(self) -> dict[str, float | None]:\n        return {\n            \"connect\": self.connect,\n            \"read\": self.read,\n            \"write\": self.write,\n            \"pool\": self.pool,\n        }", "summary": "Serialises a Timeout to a plain dict for request extensions.", "kind": "function"}]}
{"claim": "Proxy.raw_auth returns a (username, password) bytes tuple when the proxy URL carries credentials.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 227, "end_line": 234, "symbol": "Proxy.raw_auth"}, "content": "    def raw_auth(self) -> tuple[bytes, bytes] | None:\n        # The proxy authentication as raw bytes.\n        return (\n            None\n            if self.auth is None\n            else (self.auth[0].encode(\"utf-8\"), self.auth[1].encode(\"utf-8\"))\n        )\n", "summary": "Property returning the proxy credentials as bytes.", "kind": "function"}]}
{"claim": "Client.send raises RuntimeError if the client has already been closed.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 879, "end_line": 901, "symbol": "Client.send"}, "content": "    def send(\n        self,\n        request: Request,\n        *,\n        stream: bool = False,\n        auth: AuthTypes | UseClientDefault | None = USE_CLIENT_DEFAULT,\n        follow_redirects: bool | UseClientDefault = USE_CLIENT_DEFAULT,\n    ) -> Response:\n        \"\"\"\n        Send a request.\n\n        The request is sent as-is, unmodified.\n\n        Typically you'll want to build one with `Client.build_request()`\n        so that any client-level configuration is merged into the request,\n        but passing an explicit `httpx.Request()` is supported as well.\n\n        See also: [Request instances][0]\n\n        [0]: /advanced/clients/#request-instances\n        \"\"\"\n        if self._state == ClientState.CLOSED:\n            raise RuntimeError(\"Cannot send a request, as the client has been closed.\")", "summary": "Public send entrypoint; refuses calls on a closed client.", "kind": "function"}]}
{"claim": "Limits.keepalive_expiry defaults to 5.0 seconds.", "expected_verdict": "supported", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 173, "end_line": 183, "symbol": "Limits.__init__"}, "content": "    def __init__(\n        self,\n        *,\n        max_connections: int | None = None,\n        max_keepalive_connections: int | None = None,\n        keepalive_expiry: float | None = 5.0,\n    ) -> None:\n        self.max_connections = max_connections\n        self.max_keepalive_connections = max_keepalive_connections\n        self.keepalive_expiry = keepalive_expiry\n", "summary": "Limits constructor; defaults keepalive_expiry to 5.0.", "kind": "function"}]}
{"claim": "httpx.BasicAuth stores the user's password in cleartext on the Request object.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_auth.py", "start_line": 126, "end_line": 143, "symbol": "BasicAuth"}, "content": "class BasicAuth(Auth):\n    \"\"\"\n    Allows the 'auth' argument to be passed as a (username, password) pair,\n    and uses HTTP Basic authentication.\n    \"\"\"\n\n    def __init__(self, username: str | bytes, password: str | bytes) -> None:\n        self._auth_header = self._build_auth_header(username, password)\n\n    def auth_flow(self, request: Request) -> typing.Generator[Request, Response, None]:\n        request.headers[\"Authorization\"] = self._auth_header\n        yield request\n\n    def _build_auth_header(self, username: str | bytes, password: str | bytes) -> str:\n        userpass = b\":\".join((to_bytes(username), to_bytes(password)))\n        token = b64encode(userpass).decode()\n        return f\"Basic {token}\"\n", "summary": "BasicAuth class — builds and yields a Basic Authorization header.", "kind": "function"}]}
{"claim": "The GZipDecoder uses the brotli library to decompress response bodies.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_decoders.py", "start_line": 85, "end_line": 105, "symbol": "GZipDecoder"}, "content": "class GZipDecoder(ContentDecoder):\n    \"\"\"\n    Handle 'gzip' decoding.\n\n    See: https://stackoverflow.com/questions/1838699\n    \"\"\"\n\n    def __init__(self) -> None:\n        self.decompressor = zlib.decompressobj(zlib.MAX_WBITS | 16)\n\n    def decode(self, data: bytes) -> bytes:\n        try:\n            return self.decompressor.decompress(data)\n        except zlib.error as exc:\n            raise DecodingError(str(exc)) from exc\n\n    def flush(self) -> bytes:\n        try:\n            return self.decompressor.flush()\n        except zlib.error as exc:  # pragma: no cover\n            raise DecodingError(str(exc)) from exc", "summary": "GZip content decoder using zlib.", "kind": "function"}]}
{"claim": "Client._send_handling_redirects silently truncates the response when too many redirects occur.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 964, "end_line": 999, "symbol": "Client._send_handling_redirects"}, "content": "    def _send_handling_redirects(\n        self,\n        request: Request,\n        follow_redirects: bool,\n        history: list[Response],\n    ) -> Response:\n        while True:\n            if len(history) > self.max_redirects:\n                raise TooManyRedirects(\n                    \"Exceeded maximum allowed redirects.\", request=request\n                )\n\n            for hook in self._event_hooks[\"request\"]:\n                hook(request)\n\n            response = self._send_single_request(request)\n            try:\n                for hook in self._event_hooks[\"response\"]:\n                    hook(response)\n                response.history = list(history)\n\n                if not response.has_redirect_location:\n                    return response\n\n                request = self._build_redirect_request(request, response)\n                history = history + [response]\n\n                if follow_redirects:\n                    response.read()\n                else:\n                    response.next_request = request\n                    return response\n\n            except BaseException as exc:\n                response.close()\n                raise exc", "summary": "Redirect loop that enforces a maximum redirect count.", "kind": "function"}]}
{"claim": "Limits exposes a 'max_requests_per_second' field to rate-limit outgoing traffic.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 159, "end_line": 196, "symbol": "Limits"}, "content": "class Limits:\n    \"\"\"\n    Configuration for limits to various client behaviors.\n\n    **Parameters:**\n\n    * **max_connections** - The maximum number of concurrent connections that may be\n            established.\n    * **max_keepalive_connections** - Allow the connection pool to maintain\n            keep-alive connections below this point. Should be less than or equal\n            to `max_connections`.\n    * **keepalive_expiry** - Time limit on idle keep-alive connections in seconds.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        max_connections: int | None = None,\n        max_keepalive_connections: int | None = None,\n        keepalive_expiry: float | None = 5.0,\n    ) -> None:\n        self.max_connections = max_connections\n        self.max_keepalive_connections = max_keepalive_connections\n        self.keepalive_expiry = keepalive_expiry\n\n    def __eq__(self, other: typing.Any) -> bool:\n        return (\n            isinstance(other, self.__class__)\n            and self.max_connections == other.max_connections\n            and self.max_keepalive_connections == other.max_keepalive_connections\n            and self.keepalive_expiry == other.keepalive_expiry\n        )\n\n    def __repr__(self) -> str:\n        class_name = self.__class__.__name__\n        return (\n            f\"{class_name}(max_connections={self.max_connections}, \"\n            f\"max_keepalive_connections={self.max_keepalive_connections}, \"", "summary": "Configuration object for connection-pool limits.", "kind": "function"}]}
{"claim": "Client._set_timeout overwrites any caller-provided timeout in request.extensions.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 584, "end_line": 592, "symbol": "BaseClient._set_timeout"}, "content": "    def _set_timeout(self, request: Request) -> None:\n        if \"timeout\" not in request.extensions:\n            timeout = (\n                self.timeout\n                if isinstance(self.timeout, UseClientDefault)\n                else Timeout(self.timeout)\n            )\n            request.extensions = dict(**request.extensions, timeout=timeout.as_dict())\n", "summary": "Merges the client's default timeout into request extensions.", "kind": "function"}]}
{"claim": "HTTPTransport.handle_request opens a raw TCP socket directly without delegating to httpcore.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_transports/default.py", "start_line": 230, "end_line": 259, "symbol": "HTTPTransport.handle_request"}, "content": "    def handle_request(\n        self,\n        request: Request,\n    ) -> Response:\n        assert isinstance(request.stream, SyncByteStream)\n        import httpcore\n\n        req = httpcore.Request(\n            method=request.method,\n            url=httpcore.URL(\n                scheme=request.url.raw_scheme,\n                host=request.url.raw_host,\n                port=request.url.port,\n                target=request.url.raw_path,\n            ),\n            headers=request.headers.raw,\n            content=request.stream,\n            extensions=request.extensions,\n        )\n        with map_httpcore_exceptions():\n            resp = self._pool.handle_request(req)\n\n        assert isinstance(resp.stream, typing.Iterable)\n\n        return Response(\n            status_code=resp.status,\n            headers=resp.headers,\n            stream=ResponseStream(resp.stream),\n            extensions=resp.extensions,\n        )", "summary": "Sync transport: forwards request to httpcore connection pool.", "kind": "function"}]}
{"claim": "codes.is_success returns True for any status code less than 400, including redirects.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_status_codes.py", "start_line": 53, "end_line": 59, "symbol": "codes.is_success"}, "content": "    def is_success(cls, value: int) -> bool:\n        \"\"\"\n        Returns `True` for 2xx status codes, `False` otherwise.\n        \"\"\"\n        return 200 <= value <= 299\n\n    @classmethod", "summary": "Classifier helper returning True for 200..299 status codes.", "kind": "function"}]}
{"claim": "codes.is_redirect returns True only for status code 301.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_status_codes.py", "start_line": 60, "end_line": 66, "symbol": "codes.is_redirect"}, "content": "    def is_redirect(cls, value: int) -> bool:\n        \"\"\"\n        Returns `True` for 3xx status codes, `False` otherwise.\n        \"\"\"\n        return 300 <= value <= 399\n\n    @classmethod", "summary": "Classifier helper for 300..399 status codes.", "kind": "function"}]}
{"claim": "ZStandardDecoder ships its own bundled zstd C extension and does not require an external package.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_decoders.py", "start_line": 161, "end_line": 201, "symbol": "ZStandardDecoder"}, "content": "class ZStandardDecoder(ContentDecoder):\n    \"\"\"\n    Handle 'zstd' RFC 8878 decoding.\n\n    Requires `pip install zstandard`.\n    Can be installed as a dependency of httpx using `pip install httpx[zstd]`.\n    \"\"\"\n\n    # inspired by the ZstdDecoder implementation in urllib3\n    def __init__(self) -> None:\n        if zstandard is None:  # pragma: no cover\n            raise ImportError(\n                \"Using 'ZStandardDecoder', ...\"\n                \"Make sure to install httpx using `pip install httpx[zstd]`.\"\n            ) from None\n\n        self.decompressor = zstandard.ZstdDecompressor().decompressobj()\n        self.seen_data = False\n\n    def decode(self, data: bytes) -> bytes:\n        assert zstandard is not None\n        self.seen_data = True\n        output = io.BytesIO()\n        try:\n            output.write(self.decompressor.decompress(data))\n            while self.decompressor.eof and self.decompressor.unused_data:\n                unused_data = self.decompressor.unused_data\n                self.decompressor = zstandard.ZstdDecompressor().decompressobj()\n                output.write(self.decompressor.decompress(unused_data))\n        except zstandard.ZstdError as exc:\n            raise DecodingError(str(exc)) from exc\n        return output.getvalue()\n\n    def flush(self) -> bytes:\n        if not self.seen_data:\n            return b\"\"\n        ret = self.decompressor.flush()  # note: this is a no-op\n        if not self.decompressor.eof:\n            raise DecodingError(\"Zstandard data is incomplete\")  # pragma: no cover\n        return bytes(ret)\n", "summary": "Zstandard content decoder, RFC 8878.", "kind": "function"}]}
{"claim": "DigestAuth.auth_flow falls back to HTTP Basic credentials whenever the server replies with 401.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_auth.py", "start_line": 193, "end_line": 222, "symbol": "DigestAuth.auth_flow"}, "content": "    def auth_flow(self, request: Request) -> typing.Generator[Request, Response, None]:\n        if self._last_challenge:\n            request.headers[\"Authorization\"] = self._build_auth_header(\n                request, self._last_challenge\n            )\n\n        response = yield request\n\n        if response.status_code != 401 or \"www-authenticate\" not in response.headers:\n            # If the response is not a 401 then we don't\n            # need to build an authenticated request.\n            return\n\n        for auth_header in response.headers.get_list(\"www-authenticate\"):\n            if auth_header.lower().startswith(\"digest \"):\n                break\n        else:\n            # If the response does not include a 'WWW-Authenticate: Digest ...'\n            # header, then we don't need to build an authenticated request.\n            return\n\n        self._last_challenge = self._parse_challenge(request, response, auth_header)\n        self._nonce_count = 1\n\n        request.headers[\"Authorization\"] = self._build_auth_header(\n            request, self._last_challenge\n        )\n        if response.cookies:\n            Cookies(response.cookies).set_cookie_header(request=request)\n        yield request", "summary": "Digest auth flow that retries on 401 with computed credentials.", "kind": "function"}]}
{"claim": "Client._send_single_request automatically retries the request on a network error before returning.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 1001, "end_line": 1035, "symbol": "Client._send_single_request"}, "content": "    def _send_single_request(self, request: Request) -> Response:\n        \"\"\"\n        Sends a single request, without handling any redirections.\n        \"\"\"\n        transport = self._transport_for_url(request.url)\n        start = time.perf_counter()\n\n        if not isinstance(request.stream, SyncByteStream):\n            raise RuntimeError(\n                \"Attempted to send an async request with a sync Client instance.\"\n            )\n\n        with request_context(request=request):\n            response = transport.handle_request(request)\n\n        assert isinstance(response.stream, SyncByteStream)\n\n        response.request = request\n        response.stream = BoundSyncStream(\n            response.stream, response=response, start=start\n        )\n        self.cookies.extract_cookies(response)\n        response.default_encoding = self._default_encoding\n\n        logger.info(\n            'HTTP Request: %s %s \"%s %d %s\"',\n            request.method,\n            request.url,\n            response.http_version,\n            response.status_code,\n            response.reason_phrase,\n        )\n\n        return response\n", "summary": "Send a single request, no redirect handling.", "kind": "function"}]}
{"claim": "Timeout.as_dict returns a dict keyed by 'total' and 'idle' instead of connect/read/write/pool.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 132, "end_line": 138, "symbol": "Timeout.as_dict"}, "content": "    def as_dict(self) -> dict[str, float | None]:\n        return {\n            \"connect\": self.connect,\n            \"read\": self.read,\n            \"write\": self.write,\n            \"pool\": self.pool,\n        }", "summary": "Serialises a Timeout to a plain dict for request extensions.", "kind": "function"}]}
{"claim": "Proxy.raw_auth returns the proxy URL's full bearer token for OAuth proxies.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 227, "end_line": 234, "symbol": "Proxy.raw_auth"}, "content": "    def raw_auth(self) -> tuple[bytes, bytes] | None:\n        # The proxy authentication as raw bytes.\n        return (\n            None\n            if self.auth is None\n            else (self.auth[0].encode(\"utf-8\"), self.auth[1].encode(\"utf-8\"))\n        )\n", "summary": "Property returning the proxy credentials as bytes.", "kind": "function"}]}
{"claim": "Client.send opens a new TCP connection for every call, ignoring connection pooling.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_client.py", "start_line": 879, "end_line": 927, "symbol": "Client.send"}, "content": "    def send(\n        self,\n        request: Request,\n        *,\n        stream: bool = False,\n        auth: AuthTypes | UseClientDefault | None = USE_CLIENT_DEFAULT,\n        follow_redirects: bool | UseClientDefault = USE_CLIENT_DEFAULT,\n    ) -> Response:\n        \"\"\"\n        Send a request.\n\n        The request is sent as-is, unmodified.\n\n        Typically you'll want to build one with `Client.build_request()`\n        so that any client-level configuration is merged into the request,\n        but passing an explicit `httpx.Request()` is supported as well.\n\n        See also: [Request instances][0]\n\n        [0]: /advanced/clients/#request-instances\n        \"\"\"\n        if self._state == ClientState.CLOSED:\n            raise RuntimeError(\"Cannot send a request, as the client has been closed.\")\n\n        self._state = ClientState.OPENED\n        follow_redirects = (\n            self.follow_redirects\n            if isinstance(follow_redirects, UseClientDefault)\n            else follow_redirects\n        )\n\n        self._set_timeout(request)\n\n        auth = self._build_request_auth(request, auth)\n\n        response = self._send_handling_auth(\n            request,\n            auth=auth,\n            follow_redirects=follow_redirects,\n            history=[],\n        )\n        try:\n            if not stream:\n                response.read()\n\n            return response\n\n        except BaseException as exc:\n            response.close()", "summary": "Public send entrypoint; refuses calls on a closed client.", "kind": "function"}]}
{"claim": "Limits.keepalive_expiry defaults to 60 seconds.", "expected_verdict": "rejected", "chunks": [{"ref": {"file_path": "httpx/_config.py", "start_line": 173, "end_line": 183, "symbol": "Limits.__init__"}, "content": "    def __init__(\n        self,\n        *,\n        max_connections: int | None = None,\n        max_keepalive_connections: int | None = None,\n        keepalive_expiry: float | None = 5.0,\n    ) -> None:\n        self.max_connections = max_connections\n        self.max_keepalive_connections = max_keepalive_connections\n        self.keepalive_expiry = keepalive_expiry\n", "summary": "Limits constructor; defaults keepalive_expiry to 5.0.", "kind": "function"}]}
